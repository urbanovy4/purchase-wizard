import { HideCardNumberPipe } from './hide-card-number.pipe';

describe('HideCardNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new HideCardNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
